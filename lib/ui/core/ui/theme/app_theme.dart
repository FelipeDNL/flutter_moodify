import 'package:flutter/material.dart';

class AppTheme {
    static final Color primary = Color(0xFF00C3D0).withValues(alpha: 0.75);
    static final Color surface = Color(0xFF131927);

    // Text Colors
    static final Color onSurface = Color(0xFFFFFFFF);
}